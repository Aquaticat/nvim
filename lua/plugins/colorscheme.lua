-- No colorscheme plugin needed. The PoC theme is applied via highlight
-- commands at the end of init.lua. This file exists so lazy.nvim's
-- "plugins" directory scan doesn't break if it expects at least one file
-- per convention. Returning an empty table is valid for lazy.nvim.
return {}
